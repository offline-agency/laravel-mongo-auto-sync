{
    "name": "offline-agency/laravel-mongo-auto-sync",
    "description": "A package that allows to sync changes between collections on MongoDB project.",
    "type": "library",
    "keywords": ["laravel","database","mongodb","eloquent"],
    "require": {
        "php": ">=7.0",
        "ext-json": "*",
        "ext-mongodb": "*",
        "evercode1/trait-maker": "^1.0",
        "jenssegers/mongodb": "^3.3"
    },
    "require-dev": {
        "mockery/mockery": "^1.0",
        "orchestra/testbench": "~3.7.0",
        "phpunit/phpunit": "^7.5"
    },
    "license": "MIT",
    "authors": [
        {
            "name": "Offline Agency",
            "email": "info@offlineagency.it"
        }
    ],
    "autoload": {
        "psr-4": {
            "OfflineAgency\\MongoAutoSync\\": "src/"
        },
        "files": [
            "src/syncUtils.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit"
    },
    "config": {
        "preferred-install": "dist",
        "sort-packages": true,
        "optimize-autoloader": true
    },
    "extra": {
        "laravel": {
            "providers": [
                "OfflineAgency\\MongoAutoSync\\Providers\\ServiceProvider"
            ]
        }
    }
}
